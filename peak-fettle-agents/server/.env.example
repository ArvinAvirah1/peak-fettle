# Peak Fettle API — environment variables
# Copy to .env and fill in.

# Postgres connection string from Supabase Project Settings → Database → URI
SUPABASE_DB_URL=postgres://postgres:PASSWORD@HOST:5432/postgres

# Supabase project URL — from Project Settings → API → Project URL
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Supabase service role key — from Project Settings → API → service_role secret.
# NEVER expose this to the client. Server-side only.
# Required for: auth.admin.deleteUser() (TICKET-030) and any privileged admin ops.
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key-from-supabase-dashboard

# JWT signing secret — generate with: openssl rand -hex 64
JWT_SECRET=replace-with-64-bytes-of-randomness

# CORS origin for the web mirror; use * for local dev
WEB_ORIGIN=http://localhost:5173

# Server port
PORT=4000

# Optional: Sentry DSN, PostHog key — added in next backlog item
SENTRY_DSN=
POSTHOG_KEY=

# RevenueCat webhook shared secret — REQUIRED in production. The webhook
# fails closed (503) until this is set; paste the same value into the
# RevenueCat dashboard webhook's Authorization header field.
REVENUECAT_WEBHOOK_AUTH=
# Optional: RevenueCat secret API key for server-side subscriber lookups.
REVENUECAT_SECRET_KEY=
