# SPEC — Final Run: 094-A Data-Layer Move + On-Device Ports + 093 Lens-1 + 099 (BINDING)

**Working rules:** identical to TRAINING_ENGINE_SPEC §0 — bash-heredoc writes ONLY (Write/Edit corrupt this mount), verify wc -lc + tail, /tmp backups, no rm/mv/git, node --check + @babel parse as DoD per agent. **mobile/package.json is corrupted on this mount — never read, patch, or npm-install against it. No new dependencies anywhere.** For node tests of mobile TS: copy files to /tmp with a stub `{"name":"t"}` package.json and NODE_PATH to server node_modules (proven pattern). **Final agent reports: ≤25 lines.**

## Governing decisions
tierPolicy.ts is the single branch point: **Pro (`is_paid`) = server data + live PowerSync (unchanged)**; **free = on-device SQLite only** + encrypted blob backup (already shipped). Percentiles: model-calibrated, computed on-device for everyone (D6). Plan generation: engine logic available on-device; the Pro gate on generation REMAINS (UI-level). Insights: on-device for free, server endpoints remain for Pro. Groups: ALL clients send a tiny weekly signal; the server never needs personal logs for group math.

## Ownership matrix (STRICT — cross-needs go in the report)
| Agent | Files |
|---|---|
| L | `mobile/src/db/localSchema.ts`, new `mobile/src/db/migrations.ts`, `mobile/src/data/backup/exportEngine.ts`, new `mobile/src/db/__tests__/migrations.test.js` |
| M | new `mobile/src/lib/trainingEngine/*` (port), new `mobile/src/lib/insightsLocal.ts`, new tests for both |
| N | `mobile/src/lib/strengthModelV3.ts`, `mobile/__tests__/strength-model-v3.test.js`, `mobile/scripts/deriveStrengthModelV3.py`, `mobile/app/(tabs)/rankings.tsx` |
| O | `sync-rules.yaml`, `server/routes/groups.js`, new `server/migrations/20260612_group_weekly_signals.sql`, new `server/migrations/20260612_drop_percentile_rankings.sql` (POST-VERIFICATION file — do not fold into schema.sql), `server/cron/*` (disable percentile cron only), `server/index.js` (cron disable only) |
| P | `mobile/src/hooks/*` (the data hooks), `mobile/src/api/client.ts` NOT allowed — wrap at hook level only; `mobile/src/data/*` non-backup files as needed |
| Q | new `server/migrations/20260612_oauth_identities.sql`, `db/schema.sql` (fold 20260611 engine/exercise migrations + oauth_identities — NOT the drop file), new `mobile/src/components/auth/OAuthButtons.tsx`, `mobile/app/(auth)/login.tsx` + `register.tsx`, `tickets/TICKET-078-*.md` + `tickets/TICKET-094-*.md` (status notes only) |

## Agent L — local schema v2 + migration runner
localSchema v2 adds (TEXT pk id, snake_case cols mirroring server, JSON-as-TEXT for jsonb, ISO TEXT dates): `plans, routines, workout_templates, template_sessions, template_exercises, streaks, streak_overrides, daily_health_log, daily_health_metrics, habits, user_weekly_goals, user_constraints, exercise_prs, user_confirmed_1rm, user_cosmetics, user_equipped_cosmetics, user_profile` (single-row, the §A profile fields incl. the six survey fields). Keep ALL existing tables/statements untouched.
`migrations.ts`: ordered list `[{v:2, statements:[...]}]`, runner reads `PRAGMA user_version`, applies in tx where possible, sets version; **pre-migration snapshot**: try expo-file-system (dynamic require) write of `buildBackupFromDb` JSON to documentDirectory `pf_premigration_v<N>.json`; if FS unavailable, write into a `migration_snapshots(version, created_at, payload)` table FIRST (best-effort, never blocks). Wire `localDb.init()` to run the runner after base statements.
exportEngine: extend `BACKUP_TABLES` with all new tables; bump `BACKUP_SCHEMA_VERSION` 1→2 with an up-migration (v1 docs restore: missing tables = empty arrays). Tests: runner idempotence (run twice = same version), v1→v2 backup up-migration, fresh-install creates all tables.

## Agent M — engine + insights on-device port
Port `peak-fettle-agents/server/lib/trainingEngine/{templates,scaleDown,sequence,exerciseFill,loading,reasoning,index}.js` → `mobile/src/lib/trainingEngine/*.ts` FAITHFULLY (same exported API, same ruleTrace strings; typed). Read the server files as source of truth.
`insightsLocal.ts`: pure functions `computeRecovery(sets14d, now)`, `computeReadiness(metrics28d, tonnage7d, tonnage28d)`, `computeDeload(history, lastDeloadAt)` implementing the EXACT formulas/weights/bands of TRAINING_ENGINE_SPEC §4 (read it). Inputs are plain arrays (callers query localDb) — no DB access in the module.
Tests in /tmp pattern: port 6+ engine cases from `server/__tests__/trainingEngine.test.js` and assert key invariants match server behavior (discipline coverage, constraint exclusion, determinism, 15-min recipe); readiness band boundary cases; recovery tau math.

## Agent N — 093 Lens-1 revisions + rankings wiring
In strengthModelV3.ts (read its current §PROVENANCE block first): (1) **heteroscedastic σ** — σ varies by experience tier per the v3 doc §4 (read `strength_model_v3_revision_2026-06-06.md` §4 for the specified form; if §4 only sketches it, implement σ(q) linear interpolation across anchors and document); (2) **age adjustment** — McCulloch/Foster-style multiplier table for masters (40+) and youth, applied to the user's lift before percentile lookup, OFF when age_band unknown; (3) **per-lift α** (allometric exponent) — fit per lift from the standards table instead of a global α; re-derive constants via the derivation script (extend it; keep provenance block in sync). Update tests (keep 45 green, add age/α/σ cases).
rankings.tsx: per-lift lens percentiles switch from server values to the on-device model (`strengthModelV3`) — compute from the user's e1RM + bodyweight + sex + age_band locally; keep server values as fallback when local inputs missing. Respect last run's D8-UI suppression (TierLadderCard untouched).

## Agent O — server slim + group signals
`20260612_group_weekly_signals.sql`: `group_weekly_signals(id uuid pk default, group_id fk, user_id fk, week_start date, hit_goal bool, workouts_done smallint, created_at)`, UNIQUE(group_id,user_id,week_start), idempotent.
groups.js: add `POST /groups/:id/weekly-signal` (upsert own row; validate week_start is a Monday or normalize) + rework the weekly evaluation path to compute `group_week_evaluations`/`credit_ledger` FROM signals (keep legacy log-based path as fallback if no signals exist for the week, clearly commented). 
sync-rules.yaml: gate the `user_private` bucket's personal tables to paid only — parameter query `select request.user_id() as user_id from users where id = request.user_id() and tier = 'paid'`; keep group + global_library buckets for everyone. Add header comment explaining tier gating.
Percentile cron: disable its schedule (comment out registration in index.js or cron loader with a dated note), keep files. `20260612_drop_percentile_rankings.sql`: DROP TABLE user_percentile_rankings + the batch fn + v_user_lift_inputs, header: "APPLY ONLY AFTER device-test verification — founder gate." node --check + jest on any touched tests.

## Agent P — data-access tier branching
Read tierPolicy.ts, localDb.ts, and EVERY hook in mobile/src/hooks before editing. For each personal-data hook (useWorkout, useWorkoutHistory, usePlans, useStreak, usePowerSyncWorkout consumers, health/goals/constraints hooks as found): branch — `isLocalFirst(user)` ⇒ read/write localDb (tables from Agent L's schema; code against the table names in this spec) and DO NOT call personal REST endpoints; Pro ⇒ unchanged current behavior. Writes on free path skip outbox/server entirely. usePlans.generate: Pro+online ⇒ server (unchanged); Pro+offline ⇒ local engine (`mobile/src/lib/trainingEngine`, Agent M contract: same API as server module) with a "generated offline" note in ruleTrace; free ⇒ existing upsell (unchanged). Insights consumers (insights.tsx api calls happen via api/insights.ts — you may add a thin branch INSIDE the hook/screen-level data fetch if a hook exists; otherwise add `mobile/src/hooks/useInsights.ts` wrapping the branch: free ⇒ insightsLocal + localDb queries, Pro ⇒ existing endpoints) — insights.tsx itself belongs to no one this run; if it must change imports, report it instead of editing.
Group weekly signal: on free AND pro, after a workout completes (find the completion path in the logger host hook chain — likely useWorkout/usePowerSyncWorkout), fire-and-forget POST `/groups/:id/weekly-signal` for each of the user's groups (read group ids from whatever group hook/state exists; if not cleanly accessible, implement `maybeSendWeeklySignals()` in a new `mobile/src/data/groupSignals.ts` you own, called from the workout-save path, and report the wiring point).
Every changed hook: preserve exported API exactly (signature-compatible). Parse-clean.

## Agent Q — 099 + hygiene
`20260612_oauth_identities.sql`: `oauth_identities(id uuid pk, user_id fk users, provider text check in ('apple','google'), provider_sub text, created_at)`, UNIQUE(provider, provider_sub), idempotent. Fold INTO `db/schema.sql` as well (per the standing rule), AND fold the two 20260611 migration files' contents into schema.sql (users survey columns; exercises movement_pattern/equipment + tagging UPDATEs as a seed section) — read schema.sql conventions first; do NOT fold the drop-percentile file.
`OAuthButtons.tsx`: renders Apple + Google buttons ONLY if `expo-apple-authentication` / `expo-auth-session` resolve via guarded dynamic require (try/catch → render null + one console.warn); on success call the existing `oauthLogin(provider, idToken)` (find it in AuthContext/api — read first). Mount in login.tsx + register.tsx under the existing form with a divider. Zero hard imports of the uninstalled packages (bundle-safe today, lights up after founder installs).
Tickets hygiene: TICKET-078 status header → "✅ FIXED + COMMITTED (2026-06-12) — verified by parse-sweep"; append to TICKET-094 a dated status block: Workstream B shipped 57b19a7; Workstream A this run; drop-file founder-gated. ≤25-line report.

## Acceptance (reviewer)
1. Full parse sweep + node --check clean. 2. L/M/N tests green (/tmp pattern). 3. grep: no personal REST calls on the free path of touched hooks (`isLocalFirst` guards present); exported hook APIs unchanged. 4. sync-rules personal buckets tier-gated; groups signal endpoint matches spec shape. 5. No new deps; package.json untouched. 6. insightsLocal numbers == spec §4 (spot-check weights/bands/tau). 7. schema.sql parses (psql-style sanity: balanced quotes, ends with ;) and contains survey cols + movement_pattern + oauth_identities. 8. Engine port: exported API parity with server module (diff the export lists).
