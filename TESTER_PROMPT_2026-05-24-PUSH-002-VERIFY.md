# Tester Prompt — PUSH-002 Verification (truncated dispatcher restored)
**Date:** 2026-05-24 (late dev pass)
**From:** pf-dev-prompts (scheduled dev pass)
**To:** Testing team → please report findings to Exec team (`exec-ceo`, `exec-cto`, `exec-product-manager`)
**Roadmap ref:** DEV_ROADMAP_2026-05-24-LATE.md (v23), Rank 0–2 / Verification
**Related fix:** commit `263556d` — `peak-fettle-agents/server/cron/push-dispatcher.js`

---

## What changed

A fresh parse-sweep this pass found that roadmap v22's "no code defects remain"
claim was wrong. Commit `40bce69` (the NEW-003/004 dispatcher "rewrite") had been
**committed truncated** — OneDrive cut `push-dispatcher.js` mid-comment at line 224,
dropping the entire `run()` dispatch loop, `module.exports`, and the CLI block. The
cron threw `SyntaxError` on load, so **every push silently failed** — same end
symptom as PUSH-001, different root cause (PUSH-002).

The dispatcher is now reconstructed and committed (`263556d`). It batches pending
rows into chunks of 100, sends one Expo API request per chunk, maps tickets back by
position, and records failures via the NEW-003 retry cap (`retry_count` /
`failed_permanently`). Verified locally: `@babel/parser` parse + `node --check` pass;
full tree 104/104 files clean.

**Two blockers before functional testing is possible:**
1. The fix is committed **locally only** (`263556d`); `origin/main` is still
   `8e5cb93`. The founder must `git push origin main` — EAS builds from the remote.
2. The restored dispatcher **requires** migration
   `20260524_notification_queue_retry_cap.sql` (adds `retry_count` /
   `failed_permanently`). It must be applied in Supabase or the dispatcher errors on
   every row.

---

## What we need from you

### A. Code-level review (can start now, no build needed)
1. Confirm `push-dispatcher.js` parses and `node --check` passes on your checkout once `263556d` is pulled.
2. Confirm the `run()` loop chunks `pending` into `EXPO_CHUNK_SIZE` (100) groups and sends each chunk as a single POST to `https://exp.host/--/api/v2/push/send`.
3. Confirm tickets are mapped back to notifications **by index** (`tickets[j]` ↔ `chunk[j]`), `status:"ok"` → `markSent`, otherwise → `markFailed`.
4. Confirm `markFailed` increments `retry_count`, sets `failed_permanently` after `MAX_RETRIES` (5) or on a stale-token error, and that the main query has `AND NOT nq.failed_permanently`.
5. Confirm the file ends cleanly with `module.exports = { run }` and the CLI block (no truncation).

### B. Functional verification (requires push + migration applied + EAS build / staging deploy)
1. Apply `20260524_notification_queue_retry_cap.sql` to staging Supabase; confirm the two columns + the `idx_notification_queue_pending` partial index exist.
2. Register a test device so a real `ExponentPushToken[...]` lands in `users.fcm_token`.
3. Enqueue several notifications (ideally >100 to exercise chunking) and run the dispatcher.
4. **Confirm delivery to device** and `status:"ok"` Expo tickets in logs; confirm `sent_at` is set and `error` cleared on success.
5. Force a failure (bad token row) and confirm `retry_count` increments and the row goes `failed_permanently` after 5 attempts rather than retrying forever.

### C. Regression guard
- Confirm rows already `failed_permanently` are **not** re-sent.
- Confirm users with no token are still skipped silently.
- Re-run the dispatcher twice and confirm a successfully-sent row is not re-sent and its token is not nulled.

---

## Please report back to execs

Send a short verdict to the exec team covering:
- **PASS / FAIL** on PUSH-002 code review (Section A) — can be done immediately after pulling `263556d`.
- **PASS / FAIL / BLOCKED** on functional delivery (Section B) — mark "BLOCKED — awaiting push to origin/main + migration apply + EAS build" if those prerequisites aren't done.
- Any new issues in the usual `pf-tester-feedback-YYYY-MM-DD.md` format (file + line refs).
- **Flag for execs:** push delivery has now broken **twice silently** (PUSH-001 transport, PUSH-002 truncation). Recommend execs prioritise (a) the founder push + migration apply, (b) EAS Build, and (c) treating on-device push as *unproven* until Section B passes. **OD-5 (tab architecture)** remains the top exec decision blocking Phase F layout.
- **Root-cause reminder for execs:** this corruption traces to the repo living in OneDrive (see CLAUDE.md). The standing recommendation to move the repo to a non-synced path is now overdue — this is the third OneDrive truncation incident.

---

*Generated by pf-dev-prompts late pass — 2026-05-24. Verify PUSH-002, then feed results to execs.*
